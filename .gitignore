node_modules/
dist/
.astro/
*.db
hone
.env
.env.*

# Secrets and license — never commit (real values live on disk / in /run/secrets)
secrets/
*.license
stdout.license

playwright-report/
test-results/
marketing-assets/
playwright/.auth/
tests/playwright/.auth/

# SQLite runtime files (local state, never commit)
*.db-wal
*.db-shm

# Local Suricata TOOL1 deploy (scripts/deploy-suricata-integration.sh when not root)
.deploy-suricata/
.aider*

# Test coverage and reports
coverage/
lighthouse-reports/
